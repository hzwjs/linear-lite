# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

项目团队成员在通勤、会议间隙或离开电脑时，通过手机查看项目任务、创建和推进任务，处理评论与通知。

## Product Purpose

Linear Lite 是轻量团队任务协作工具。移动端用于完成高频、短路径的任务执行与协作；成功意味着用户无需缩放或理解桌面布局，也能在几次触控内找到任务并完成关键更新。

## Positioning

PC 端承担项目级任务规划和完整管理，移动端仍以项目为一级上下文，提供精简后的项目任务管理。两端共享数据、术语和品牌，但使用独立页面与交互结构。

## Operating Context

移动端常用于单手操作、短时查看、现场沟通和通知跳转。首屏围绕当前项目、项目任务列表、搜索、筛选和创建任务展开，任务详情采用全屏推进式导航。

## Capabilities and Constraints

- PC 页面源码和既有功能保持不变，按设备宽度进入独立移动端页面。
- 移动端保留：项目切换、项目任务列表、搜索、简单筛选、创建任务、任务详情、核心字段更新、子任务、评论、附件、收藏与通知。
- 移动端默认启用“只看我的任务”，用户可切换为查看当前项目的全部任务；这只是默认筛选，不改变项目级任务模型。
- 移动端不展示：看板、甘特图、Analytics、批量操作、拖拽排序、显示配置、项目管理与任务导入。
- 移动 Web 复用现有 Vue、Pinia、路由和后端 API，不引入另一套数据模型。

## Brand Commitments

延续现有产品界面的 Linear-Lite 风格：浅色工作区、白色内容面、灰蓝主操作、克制状态色、细分隔线和紧凑的系统字体。简洁美观优先，不使用营销页式装饰。

## Evidence on Hand

现有 PC 端实现、设计变量、真实任务数据结构及 API 是功能和视觉事实来源。当前没有需要伪造的客户背书、商业指标或品牌影像资产。

## Product Principles

- 移动端保留项目级任务语义，不引入个人日程或每日任务模型。
- 移动优先完成任务，不复刻桌面信息架构。
- 高频动作近手可达，关键路径短且结果明确。
- 信息密度服从可读性，状态和截止时间优先于管理型字段。
- 两端共享产品语言与视觉身份，但允许不同的交互形态。
- 空态、加载、失败和离线式缓存状态都必须可理解、可恢复。

## Accessibility & Inclusion

核心触控目标至少 44×44 像素；支持安全区、键盘焦点、语义标签、减少动态效果和 15 像素以上正文；在 320–1024 像素宽度不得产生横向滚动。
