---
description: 本项目禁止在数据库中使用外键约束
globs: "**/*.sql,**/schema*.sql,linear-lite-server/**"
alwaysApply: true
---

# 禁止使用数据库外键

本项目中**不允许使用数据库外键（FOREIGN KEY）**。

## 要求

- 建表、迁移脚本、ALTER 语句中**不得**声明 `FOREIGN KEY`、`REFERENCES` 等外键约束。
- 表与表之间的关联仅通过**逻辑上的 id 字段**（如 `parent_id`、`project_id`）在应用层维护引用关系与一致性。
- 需要“按父查子”或“按项目查任务”时，在应用代码或 ORM 中通过 where 条件查询，不依赖数据库级外键。

## 正确做法示例

```sql
-- 允许：仅用列存关联 id，无 FOREIGN KEY
ALTER TABLE tasks ADD COLUMN parent_id BIGINT NULL COMMENT '父任务 ID';
CREATE INDEX idx_tasks_parent_id ON tasks (parent_id);
```

## 错误做法示例

```sql
-- 禁止：不要添加外键约束
ALTER TABLE tasks ADD CONSTRAINT fk_tasks_parent FOREIGN KEY (parent_id) REFERENCES tasks (id);
```

## 删除/级联行为

父记录被删除时，子记录中关联字段（如 `parent_id`）的更新或级联删除由**应用层**在 Service 中处理，不在 DDL 中通过 `ON DELETE CASCADE` / `ON DELETE SET NULL` 实现。
