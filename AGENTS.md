# 工作方式

以完整、可验证、可评审的结果为交付单位；完成后说明做了什么、原因和权衡，避免过程噪音。优先保证功能真实可用并通过相关编译、测试和类型检查，其次遵循代码库既有模式，再落实用户明确指令。仅当真实歧义会导致相反结果时询问；可逆实现细节和任务内后续步骤自行判断并完成。

# 目录规划
所有非代码和配置文件类的文件都落到跟目录的 docs 文件夹中。

# Linear Lite 对应关系
本项目对应 Linear Lite 中的 Linear-Lite 项目。

# 数据脚本
增量脚本最终都要归档到 `schema.sql`。

# 提交
除非特别要求，不提交本地 `application*` 配置。脏工作区只暂存本任务文件或 hunk，保留用户已有改动。
提交前根据最终暂存 diff 确认消息，固定使用 `<type>: <中文描述>` 或 `<type>(<scope>): <中文描述>`；type 常用 `feat`、`fix`、`chore`，scope 为简短英文。描述必须写明业务对象和最终行为，禁止“修复/优化/调整/提交变更/BUG修复/影像”等泛化措辞；明确的需求号或问题号放入中文描述。

# UI/UX
新页面用 OpenAI frontend-skill，产品界面叠加 ui-ux-pro-max，完成后用 impeccable 审查；已有设计系统则优先 frontend-ui-standards。

# 强制约束

- 方案只写一个最优方案；信息不足以判断时先确认。内容简洁聚焦，不写讨论背景或测试验证。
- 实施前检查现有模式和依赖，采用满足需求的最简单成熟实现；先打通最小端到端，保持关注点分离，在关键位置添加注释，避免预防性抽象和临时架构。
- 数据库操作必须使用 DBX MCP；未经要求不得覆写环境变量文件或修改方案文档；不得私自使用 `jdbcTemplate`，非必要不得通过 Feign 跨库查询。
- 不保留向后兼容；过时实现直接删除，不添加兼容层或 migration。

## 单一数据路径（硬规则）

字段必须沿“数据源表字段 -> 服务 DTO 字段 -> 前端字段 -> 业务使用点”唯一传递。严禁新增或保留字段兼容、多键回退、fallback、`a || b || c`、历史字段双读双写或“先兼容后治理”。现状不满足单一路径时，停止编码并给出重构路径；发现违例必须回退实现。

